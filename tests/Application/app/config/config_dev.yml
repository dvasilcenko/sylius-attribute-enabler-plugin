imports:
    - { resource: "config.yml" }

web_profiler:
  toolbar: true
  intercept_redirects: false

framework:
    router:
        resource: "%kernel.project_dir%/app/config/routing_dev.yml"
    profiler:
        enabled: true
